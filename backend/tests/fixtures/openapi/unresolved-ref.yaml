openapi: 3.0.3
info:
  title: Unresolved Ref Example
  version: 1.0.0
paths:
  /widgets:
    get:
      operationId: listWidgets
      responses:
        "200":
          description: A widget
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DoesNotExist"
